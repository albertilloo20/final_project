import { PointReplacerPipe } from './point-replacer.pipe';

describe('PointReplacerPipe', () => {
  it('create an instance', () => {
    const pipe = new PointReplacerPipe();
    expect(pipe).toBeTruthy();
  });
});
